dentification des symboles Commande utilisée : strings -d Intro Résultat : on identifie le label secret_xor et une string encodée Zkhinolmbc. Pas de flag en clair, le binaire utilise un XOR avec une clé 1 byte.
Analyse du binaire avec objdump objdump -d Intro Le programme lit l'entrée via stdin dans buf à 0x40202c. Il vérifie que la longueur est exactement 16 caractères. Ensuite il applique input[i] XOR key et compare avec les bytes encodés stockés à 0x402019. La clé 1 byte est stockée à 0x402018.
Extraction du flag avec gdb Breakpoint posé sur compare_loop à 0x40103e. On dump la clé : 0x402018 → 0x5a. On dump les 16 bytes encodés à 0x402019 : 0x6b 0x68 0x69 0x6e 0x6f 0x6c 0x6d 0x62 0x63 0x12 0x1b 0x19 0x11 0x12 0x1b 0x19 XOR 0x5a sur chaque byte donne le flag : 123456789HACKHAC
