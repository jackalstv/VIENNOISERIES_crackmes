 dvp un crackme en binome et att les crackme des autres eleves
crackme c'est une simulation d'une licence = returne si c'est le bon flag ou pas

mdp :

comparer avec des caractère basique
if(entrer == "azerty")
enchainement de if
if(tab[0]== "a")
if(tab[1]== "z")
if(tab[2]== "e")
....
comapraison de hash
if(hash(entrer utilisateurs) == hash(azerty))
vienoi